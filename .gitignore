# Environment
.env

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
package.json
package-lock.json

# Local app config (env-backed; không đưa lên remote)
scripts/config.py
scripts/supabase/

# OS
.DS_Store

# AI logs — tracked structure, ignored content
.ai-log/*.jsonl
.ai-log/*.md

# Docker Compose may contain local secrets — do not commit
docker-compose.yml

# Cursor IDE — local plans/settings/hooks (not for remote)
.cursor/plans/
.cursor/settings.json
.cursor/hooks.json

# Generated local test output


.vscode/
.idea/

# Local SQLite (dev / pytest)
backend/serene_local.db
backend/test_serene.db
# Raw text corpus (không version trong repo)

backend/.env
