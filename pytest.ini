[pytest]
# Session-scoped Neo4j AsyncDriver must share one event loop with async tests.
# Default test loop scope is "function", which breaks neo4j async on Windows (Proactor).
asyncio_mode = auto
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = session
